services:
  app:
    stdin_open: true
    tty: true
    image: container-hzv-qb4
    build:
      context: ./src
    ports:
      - "5000:5000"
    environment:
      PORT: "5000"
      HOSTNAME: "0.0.0.0"
      HOST: "0.0.0.0"
      NEXT_PUBLIC_BASE_URL: "http://localhost:5000"
